server:
  port: 9001

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/skincarehub
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
